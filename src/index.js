import * as ship from './modules/ship'; /* < that makes anything in that module run */
import './style.css';

console.log('ef');

const name = 'carl';

const person = { first: name };

console.log(person);

const sayHelloLinting = (
  fNam,
  asdajhdakshak,
  ajsbdkjabdkjadbkjasdsbksabdskjasbkja,
) => {
  console.log(`Hello linting, ${fName}`);
};
